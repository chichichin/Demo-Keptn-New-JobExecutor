apiVersion: "spec.keptn.sh/0.2.2"
kind: "Shipyard"
metadata:
  name: "shipyard-delivery"
spec:
  stages:
    - name: "qa"
      sequences:
        - name: "loadtest-execution"
          tasks:
            - name: "test"
              properties:
                teststrategy: performance
